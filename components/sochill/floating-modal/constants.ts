export const FLOATING_BUTTON_SIZE = 64;

// Hard-coded tab bar geometry — shared by tab bar and floating modal
export const TAB_BAR_BOTTOM = 16;   // gap from screen bottom edge
export const TAB_BAR_HEIGHT = 76;   // pill(56) + paddingTop(10) + paddingBottom(10)
